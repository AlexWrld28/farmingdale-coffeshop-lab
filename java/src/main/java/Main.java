/**
 * A placeHolder method to allow the creation of the abstract method Beverage
 *
 * we can(must) later change the name to something more appropriate
 *
 * also This intellij Project is supposed to be named "RamsBrewOrderSystem"
 *
 * But I doubt it will be a problem if we live it as it is.
 */
public class Main {
    static void main() {

    }
}
